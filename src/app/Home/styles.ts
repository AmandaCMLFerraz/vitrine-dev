import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    paddingTop: 62,
    padding: 24,
    gap: 24,
  },
  text: {
    fontSize: 32,
    fontWeight: 700,
    color: "#000",
  }
})